The best harness will...

- catch every failed tool call, to initiate an async, isolated, sliding-window-style retroactive analysis -> self-aware (i.e. knows the primitive shapes of the `harness-kit` + has access to existing harness data) self-healing proposal -> user approves/denies/requests changes flow

    -> every user review response should be captured with full relevant context and the set of these review responses should be provided to the final-gate smart model (responsible for formulating the proposal)

- ensure every session in which it was activated gets added to a Not Yet Reviewed queue

- define its own {success criteria, rubric, benchmark}
