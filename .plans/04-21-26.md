- [] Move the effect harness out of `packages` and into `harnesses`; add `harnesses` as a workspace glob

- [] Tighten the `harness-kit` around The Loop

- [] Workshop and MVP the autopoietic flows described in @../.convictions/autopoiesis.md

    - [] Errors
    - [] Full sessions
    - [] Review UI

- [] Set up `sandboxes` workspace glob, for {benchmarking, recording terminal demo gifs, validating}
